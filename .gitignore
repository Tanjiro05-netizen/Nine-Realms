# OS
.DS_Store

# Node / build (for the future Vite+TS port)
node_modules/
dist/
*.log

# Test artifacts
test-results/
playwright-report/

# Large 3D assets — track via Git LFS or keep external, not in plain git
*.glb
*.gltf
*.fbx

# ...but DO commit the web-ready optimized models the app loads at runtime
!public/models/
!public/models/*.glb
