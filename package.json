{
  "name": "nine-realms",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Nine Realms / 九州圣兵 — xianxia auto-chess (Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "playwright test",
    "assets:optimize": "node scripts/optimize-models.mjs"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.1.1",
    "@playwright/test": "^1.49.1",
    "@types/three": "^0.171.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
